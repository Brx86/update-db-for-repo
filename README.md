# build-aur-action

## Example usage
```yaml
uses: Brx86/update-db-for-repo@master
with:
  tag: x86_64
```

## Inputs
### `repo-name`
**Required** The tag of the releases to build. Default `"x86_64"`.
